{
    "name": "live-connection",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "projectType": "library",
    "sourceRoot": "libs/live-connection/src",
    "targets": {
        "lint": {
            "executor": "@nx/linter:eslint",
            "options": {
                "lintFilePatterns": [
                    "libs/live-connection/**/*.ts"
                ]
            }
        },
        "test": {
            "executor": "nx:run-commands",
            "options": {
                "command": "pnpm vitest --run --passWithNoTests",
                "cwd": "libs/live-connection"
            }
        },
        "package": {
            "executor": "nx:run-commands",
            "options": {
                "commands": [
                    "tsup src/server.ts src/browser.ts -d dist --sourcemap --format esm,cjs",
                    "tsc --emitDeclarationOnly --declaration --outDir dist"
                ],
                "cwd": "libs/live-connection",
                "parallel": false
            }
        }
    }
}