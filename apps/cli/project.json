{
    "name": "cli",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "apps/cli/src",
    "projectType": "application",
    "targets": {
        "serve": {
            "executor": "@nx/js:node",
            "defaultConfiguration": "development",
            "options": {
                "buildTarget": "cli:build",
                "args": [
                    "code-gen",
                    "--output-path",
                    "./apps/examples/dotnet-api"
                ]
            },
            "configurations": {
                "development": {
                    "buildTarget": "cli:build:development"
                },
                "production": {
                    "buildTarget": "cli:build:production"
                }
            }
        },
        "package": {
            "executor": "nx:run-commands",
            "options": {
                "commands": [
                    "tsup src/main.ts -d dist --sourcemap --format esm --clean --external @featureboard/code-generator"
                ],
                "cwd": "apps/cli",
                "parallel": false
            }
        },
        "lint": {
            "executor": "@nx/eslint:lint",
            "outputs": ["{options.outputFile}"],
            "options": {
                "lintFilePatterns": [
                    "apps/cli/**/*.ts",
                    "apps/cli/package.json"
                ]
            }
        },
        "test": {
            "executor": "@nx/vite:test",
            "outputs": ["{options.reportsDirectory}"],
            "options": {
                "passWithNoTests": true,
                "reportsDirectory": "../../coverage/apps/cli"
            }
        }
    },
    "tags": []
}
